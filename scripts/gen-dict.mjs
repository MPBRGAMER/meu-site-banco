// ══════════════════════════════════════════════════════════════// Gerador do dicionário massivo de spellcheck// Executar: node scripts/gen-dict.mjs// ══════════════════════════════════════════════════════════════import { writeFileSync } from 'fs';function unacc(s) {  return s.normalize('NFD').replace(/[̀-ͯ]/g, '');}function auto(words) {  const r = [];  const seen = new Set();  for (const w of words) {    const u = unacc(w);    if (u !== w && !seen.has(u)) { seen.add(u); r.push([u, w]); }  }  return r;}function S(obj) {  return Object.entries(obj).map(([k, v]) => [k, v]);}function M(...arrs) {  return arrs.flat();}// Será preenchido pelas partes// Cada parte exporta: { ptEntries, enEntries, ... }let allParts = [];// ══════════════════════════════════════════════════════════════// PARTE 1: PORTUGUÊS (pt) - ~800 entradas// ══════════════════════════════════════════════════════════════const ptSlang = {  